auth:
  scaling:
    replicas: 2
  containers:
    resources:
      requests:
        cpu: 0
        memory: 0

api:
  scaling:
    replicas: 2
  containers:
    resources:
      requests:
        cpu: 0
        memory: 0

web:
  scaling:
    replicas: 2
  containers:
    resources:
      requests:
        cpu: 0
        memory: 0

metricsScraper:
  scaling:
    replicas: 2
  containers:
    resources:
      requests:
        cpu: 0
        memory: 0

kong:
  proxy:
    http:
      enabled: true
  replicaCount: 2
