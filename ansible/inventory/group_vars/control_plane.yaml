# netplanロール用
netplan_configuration:
  network:
    version: 2
    ethernets:
      eth1:
        addresses:
          - 192.168.100.1{{groups["control_plane"].index(inventory_hostname)}}/24

# k3sロール用
k3s_join_command: server
k3s_join_config:
  tls-san: "{{ VIP }}"
