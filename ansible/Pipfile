[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
autoflake = "*"
pip = "*"
black = "==23.12.1"

[packages]
pyyaml = "*"
oyaml = "*"
minio = "*"
ansible = "==9.1.0"
ansible-pylibssh = "*"
passlib = "*"

[requires]
python_version = "3.12"

[scripts]
format = "sh -c \"autoflake -i --remove-all-unused-imports --remove-unused-variables -r .; black -l 110 -t py39 .;\""
ansible-setup = "ansible-galaxy install mrlesmithjr.netplan:0.4.0; pipenv run ansible-galaxy collection install vyos.vyos:4.1.0;"
