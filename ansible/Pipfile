[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
autoflake = "*"
pip = "*"
black = "==21.7b0"

[packages]
pyyaml = "*"
oyaml = "*"
minio = "*"
ansible = "==7.0.0"
ansible-pylibssh = "*"
passlib = "*"

[requires]
python_version = "3.10"

[scripts]
format = "sh -c \"autoflake -i --remove-all-unused-imports --remove-unused-variables -r .; black -l 110 -t py39 .;\""
ansible-setup = "ansible-galaxy install mrlesmithjr.netplan; pipenv run ansible-galaxy collection install vyos.vyos:2.6.0;"
